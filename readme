-- Gebruik het volgende voor de database setup in xamp:

-- CREATE TABLE IF NOT EXISTS `accounts` (
-- 	`id` int(11) NOT NULL AUTO_INCREMENT,
--   	`username` varchar(50) NOT NULL,
--   	`password` varchar(255) NOT NULL,
--   	`email` varchar(100) NOT NULL,
-- 	`registered` datetime NOT NULL,
--     PRIMARY KEY (`id`)
-- ) AUTO_INCREMENT=2 DEFAULT;

-- CREATE TABLE playlist_likes (
--     id INT AUTO_INCREMENT PRIMARY KEY,
--     user_id INT NOT NULL,
--     playlist_id VARCHAR(50) NOT NULL,
--     created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
--     UNIQUE KEY unique_like (user_id, playlist_id),
--     FOREIGN KEY (user_id) REFERENCES accounts(id) ON DELETE CASCADE
-- );

-- CREATE TABLE playlist_comments (
--     id INT AUTO_INCREMENT PRIMARY KEY,
--     user_id INT NOT NULL,
--     playlist_id VARCHAR(50) NOT NULL,
--     comment TEXT NOT NULL,
--     created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
--     FOREIGN KEY (user_id) REFERENCES accounts(id) ON DELETE CASCADE
-- );

-- verrander de database waar nodig in authenticate.php.

-- Als het nodig is zet de clientid in spotifylogin.php anders en maak je eigen app hiervoor.
-- http://127.0.0.1/ Voor als je de spotify API wilt gebruiken. Zorg ervoor dat je op poort 80 zit in xampp of verrander de redirect URI in de code.
-- Dit is de volledige link: http://127.0.0.1/fullstack-project/Fullstack-project/callback.php
-- verrander ook de clientId in spotifylogin.php en callback.php naar jouw clientId.